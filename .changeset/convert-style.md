---
'maplibrefy': minor
---

`convertStyle`: rewrite projection, hsl/hsla, pitch, distance-from-center and measure-light; drop Mapbox-only root keys, layer keys and whatever the MapLibre validator still rejects, reporting each edit as a `Change`.
