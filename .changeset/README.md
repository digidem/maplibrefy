# Changesets

Every user-facing change needs a changeset: run `npx changeset` and describe the
change from the point of view of someone reading the changelog. Version bumps
and publishing are done by the release workflow, never on feature branches.
